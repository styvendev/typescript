/* No usar nunca */
var nombre = 'Johan';

/* Usar cuando la variable cambia de valor */
let firtsName = 'Styven';

/* La mejor forma de definir variables (Constante) */
const lastName: string = 'Castro';

const fullName = `${firtsName} ${lastName}`;
console.log(nombre, fullName);
